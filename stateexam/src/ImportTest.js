const PageInfo = () => {
    return(
        <>
        
        </>
    )
}

const PageInfo2 = () => {
    return(
        <>
        
        </>
    )
}

// 여러개를 import 해야 하는 경우에는 default를 사용할 수 없음
// export를 아래와 같이 하는 경우 (1개도 마찬가지) import할 경우에도 중괄호를 이용해서 import를 진행
export {PageInfo, PageInfo2};